import 'package:flutter/material.dart';

class ConstrainApp {
  static const Duration kdefaultDuration = Duration(milliseconds: 200);
  static const Widget kdedaultLoadingWidget = CircularProgressIndicator();
}
